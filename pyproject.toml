[project]
name = "bio-sim"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "pytest>=9.1.1",
    "scipy>=1.18.1",
]
[tool.setuptools.packages.find]
include = ["bio_sim*"]
