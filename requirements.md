# バイオリアクターシミュレーター 要件定義書(ドラフト v0.1)

## 1. 概要・目的
CSTR(連続撹拌槽型リアクター)をベースとした汎用バイオプロセスシミュレーター。
回分・流加(フェドバッチ)・連続運転を切り替えて、菌体増殖・基質消費・産物生成・溶存酸素(DO)収支を数値計算し、Web UI上で可視化する。

## 2. スコープ

### 2.1 対象とする動態(In Scope)
- 菌体増殖(基質によるキネティクス、差し替え可能)
- 基質消費(収率係数・維持代謝を考慮)
- 産物生成
- 溶存酸素(DO)収支 ※2モード切替(詳細は4.4)

### 2.2 対象外(Out of Scope, 将来拡張ポイントとして設計のみ考慮)
- pH動態(酸/塩基収支、CO2ストリッピング)
- 温度動態(エネルギー収支、ジャケット冷却)
- PID制御ループ(pH・温度・DOの設定値制御)
- 多基質・多産物・多菌種(混合培養)
  → アーキテクチャ上は拡張しやすい形にしておく(下記4.6)

## 3. 運転モード
以下をユーザーが切り替え可能:

| モード | 概要 | 主な操作変数 |
|---|---|---|
| 回分(Batch) | 培地一括投入、流入・流出なし | 初期濃度 |
| 流加(Fed-batch) | 流入あり・流出なし(体積増加) | フィードプロファイル F(t)(**ユーザー定義関数を入力可能**。定数・ステップ・指数関数などはプリセットとして提供予定) |
| 連続(Continuous/CSTR) | 流入=流出、定常運転 | 希釈率 D = F/V |

## 4. モデル仕様

### 4.1 物質収支(共通ベース)
標準的なCSTR型物質収支式をベースに、運転モードに応じて流入・流出項の有無を切り替える。

- dV/dt (体積変化。回分=0、連続=0(F_in=F_out)、流加=F_in)
- dX/dt (菌体濃度: 増殖 - 希釈)
- dS/dt (基質濃度: 流入 - 消費 - 希釈)
- dP/dt (産物濃度: 生成 - 希釈)
- dC_L/dt (DO濃度: 酸素移動 - 消費 - 希釈) ※4.4参照

### 4.2 増殖キネティクス(差し替え可能設計)
- デフォルト実装: Monod式
- 拡張候補(実装 or インターフェースのみ用意): Monod+Haldane(基質阻害)、産物阻害モデル
- **設計方針**: キネティクス式を「関数/クラスとして注入(Strategyパターン)」できるようにし、μ = f(S, P, X, ...) を自由に差し替え可能にする

### 4.3 産物生成・基質消費
- 産物生成式: **汎用差し替え可能設計**(キネティクスと同様のStrategyパターン)。デフォルト実装としてLuedeking-Piret型を同梱するが、任意の式に置換可能
- 基質消費: 収率係数 Y_X/S、Y_P/S に加え、**維持代謝係数 m_S を考慮**(Pirtの式: -dS/dt = μX/Y_X/S + m_S・X)
- 菌体減少: **死滅速度 k_d を考慮**(dX/dt = μX - k_d・X - 希釈項)

### 4.4 溶存酸素(DO)収支 — 2モード切替
| モード | 内容 |
|---|---|
| **Mode A: kLa律速モード** | kLaを固定値として入力し、DO収支式 dC_L/dt = kLa(C* - C_L) - OUR を解いて動的にDO濃度を計算。DOが増殖の律速要因になり得る(低DO時の増殖阻害を表現可能) |
| **Mode B: kLa逆算モード** | DOは常に非律速(十分)と仮定して増殖・OURを計算し、OTR = OUR が成立するために必要な kLa(t) を逆算する。装置設計上必要なkLa能力の見積もりに使用 |

### 4.5 可視化・出力要件
- 時系列グラフ: X, S, P, DO(該当モード時はkLa(t)も)の推移
- 定常状態解析(連続運転時): 希釈率 vs 洗い出し点(washout point)などの解析
- CSVエクスポート機能

### 4.6 拡張性(非機能要件)
- キネティクス式・産物生成式を差し替え可能な設計(Strategy/プラグイン的構成)
- pH・温度・多基質/多産物を後付けしやすいアーキテクチャ(状態変数を辞書/データクラスで拡張可能に)

## 5. 技術スタック
- 計算エンジン: Python(NumPy / SciPy `solve_ivp` による数値積分)
- UI: Streamlit(またはDash)によるパラメータ入力・グラフ表示
- 構成: 計算エンジンとUIを分離(計算部は将来的にUIを差し替え/API化しやすいように)

## 6. 確定事項サマリ(今回のヒヤリング結果)
- 産物生成式・増殖キネティクス式は**両方とも汎用差し替え可能**(Strategyパターンで統一)
- 維持代謝係数 m_S、死滅速度 k_d を**モデルに含める**
- フィードプロファイル F(t) は**ユーザー定義関数を入力可能**(プリセットも用意)

## 7. デフォルト方針(妥当な標準として採用。異論があれば変更可)
1. **初期値・パラメータセット**: 特定の実菌株ではなく、汎用的な仮想菌株の代表値(教科書的なMonodパラメータ: μmax, Ks, Y_X/S 等)をデフォルトとし、UI上で全パラメータを自由に変更可能にする
2. **数値積分**: SciPy `solve_ivp` の `RK45`(適応刻み幅の陽的ルンゲ=クッタ法)をデフォルトとし、剛性が疑われる場合(kLa逆算モードなど)は `LSODA`(自動で剛性判定)への切替を検討
